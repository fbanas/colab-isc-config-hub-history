{
  "name": "config-hub-cicd",
  "private": true,
  "type": "module",
  "scripts": {
    "backup": "node --env-file=.env scripts/backup.mjs",
    "restore": "node --env-file=.env scripts/restore.mjs",
    "tokenize": "node scripts/tokenize.mjs"
  }
}
